# Service Configuration Overview

This document will be expanded as services are implemented. For now, ensure `pnpm install` is executed at the repo root and environment variables are supplied via `.env` files in subsequent tasks.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingestion-service
  namespace: battlescope
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingestion-service
  template:
    metadata:
      labels:
        app: ingestion-service
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: ingestion-service
          image: petdog/battlescope-ingestion:v3.0.2
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              value: "3001"
            - name: DATABASE_HOST
              value: postgres
            - name: DATABASE_PORT
              value: "5432"
            - name: DATABASE_NAME
              value: ingestion_db
            - name: DATABASE_USER
              value: battlescope
            - name: DATABASE_PASSWORD
              value: battlescope_password
            - name: KAFKA_BROKERS
              value: redpanda:9092
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: battlescope
spec:
  type: NodePort
  selector:
    app: ingestion-service
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30101

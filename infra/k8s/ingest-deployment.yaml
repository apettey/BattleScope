apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingest
  namespace: battlescope
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingest
  template:
    metadata:
      labels:
        app: ingest
    spec:
      containers:
        - name: ingest
          image: docker.io/petdog/battlescope-ingest:latest
          imagePullPolicy: Always
          command:
            - node
            - dist/backend/ingest/src/index.js
          ports:
            - name: http
              containerPort: 3002
          env:
            - name: PORT
              value: '3002'
            - name: OTEL_SERVICE_NAME
              value: 'battlescope-ingest'
          envFrom:
            - secretRef:
                name: battlescope-secrets
            - configMapRef:
                name: battlescope-config
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ingest
  namespace: battlescope
spec:
  selector:
    app: ingest
  ports:
    - name: http
      port: 3002
      targetPort: http

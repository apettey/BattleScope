apiVersion: v1
kind: Secret
metadata:
  name: typesense-secret
  namespace: battlescope
type: Opaque
stringData:
  # Change this to a strong random key in production
  # Generate with: openssl rand -base64 32
  api-key: "battlescope-search-key-change-in-production"
